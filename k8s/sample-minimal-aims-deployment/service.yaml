apiVersion: v1
kind: Service
metadata:
  name: minimal-aim-app
  labels:
    app: minimal-aim-app
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8000
  selector:
    app: minimal-aim-app