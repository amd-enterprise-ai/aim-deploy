apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-minimal-aims-helm-deployment.fullname" . }}
  labels:
    {{- include "sample-minimal-aims-helm-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-minimal-aims-helm-deployment.selectorLabels" . | nindent 4 }}

